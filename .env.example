# MongoDB Connection Settings
# Authenticated (e.g. when started with MONGO_INITDB_ROOT_USERNAME/PASSWORD):
MONGO_URI=mongodb://admin:password@localhost:27017/?authSource=admin

# Non-authenticated:
# MONGO_URI=mongodb://localhost:27017

DATABASE_NAME=notes_db

# Server Settings
PORT=8000
HOST=0.0.0.0

